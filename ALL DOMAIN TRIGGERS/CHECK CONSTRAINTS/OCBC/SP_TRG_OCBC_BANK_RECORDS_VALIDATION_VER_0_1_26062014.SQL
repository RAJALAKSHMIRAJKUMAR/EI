--version:0.1 --sdate:26/06/2014 --edate:26/06/2014 --issue:593 --desc:OBR_ACCOUNT_NUMBER,OBR_CURRENCY_TYPE,ocn_id validaiton --doneby:RL
DROP PROCEDURE IF EXISTS SP_TRG_OCBC_BANK_RECORDS_VALIDATION;
CREATE PROCEDURE SP_TRG_OCBC_BANK_RECORDS_VALIDATION(
IN ACCOUNTNUMBER INTEGER,
IN CURRENCY INTEGER,
IN OCNID INTEGER,
IN PROCESS TEXT)
BEGIN
	DECLARE MESSAGE_TEXT VARCHAR(50);
	IF(PROCESS = 'INSERT') OR (PROCESS = 'UPDATE')THEN
		IF(ACCOUNTNUMBER!=3)THEN
			SIGNAL SQLSTATE '45000'
			SET MESSAGE_TEXT='GIVE OBR_ACCOUNT_NUMBER SHOULD BE 3 ONLY';
		END IF;
		IF(CURRENCY!=2)THEN
			SIGNAL SQLSTATE '45000'
			SET MESSAGE_TEXT='GIVE OBR_CURRENCY SHOULD BE 2 ONLY';
		END IF;
		IF NOT EXISTS(SELECT OCN_ID FROM OCBC_CONFIGURATION WHERE OCN_ID=OCNID AND CGN_ID=80)THEN
			SIGNAL SQLSTATE '45000'
			SET MESSAGE_TEXT='GIVE OCN_ID BETWEEN 4 TO 8';
		END IF;
	END IF;
END;
